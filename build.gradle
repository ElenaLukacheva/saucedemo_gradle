plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(
            "org.junit.jupiter:junit-jupiter:5.9.1",
    )
}

tasks.withType(JavaCompile) {options.with
        {encoding = 'UTF-8'}
}

test {
    useJUnitPlatform()
}